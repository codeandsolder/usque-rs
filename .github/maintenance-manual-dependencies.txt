# Dependencies whose latest incompatible release cannot be resolved by generic cargo-upgrade.
# They are still checked against crates.io daily and queued separately once >=72 hours old.
quiche # quiche 0.30 removed the boringssl-vendored feature required by the current manifest; migrate features/API manually
